<div class="wrap">
  <%= paginate(@cities) %>
</div>


  <% @cities.each do |city| %>
    <div class="product">
      <div class="overlay">
        <h4><%= city.name %></h4>
        <p class="artist"><%= city.songs.count %></p>
        <!-- <p class="city"></p> -->
      </div>
      <img src="https://unsplash.it/640/640/?random">
    </div>
  <% end %>
</div>
<div class="wrap">
  <%= paginate(@cities) %>
</div>
