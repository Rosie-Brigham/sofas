<div class="wrap">
  <%= paginate(@artists) %>
</div>


  <% @artists.each do |artist| %>
    <div class="product">
      <div class="overlay">
        <h4><%= artist.name %></h4>
        <p class="artist"><%= artist.songs.count %> song in our database</p>
        <!-- <p class="city"></p> -->
      </div>
      <img src="https://unsplash.it/640/640/?random">
    </div>
  <% end %>
</div>
<div class="wrap">
  <%= paginate(@artists) %>
</div>
