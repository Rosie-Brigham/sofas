== README

## To Setup

- run `bundle install`

- run `bin/rake db:create`

- run `bin/rake db:migrate`

- run 'bin/rails runner lib/runners/populate_data.rb'

- run 'bin/rails s'

navigate to localhost:3000!

(I wanted to put a nice view for the individual entires for each city, song and artist - but I've rather run out of time and
not sure how to design it! Hopefully my approach to the index give you a vauge idea)
